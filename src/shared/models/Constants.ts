// Path
export const HOME_PATH = '/';
export const CONFIG_PATH = '/config';

// Message
export const INVALID_TOKEN_MSG = 'Current token is invalid, check the value.';
export const UNKNOWN_ERROR_MSG =
    'Apod APIs are currently unavailable, check later.';
